BootStrap: docker
From: mattocci/cmdstan-verse:4.3.2

%apprun R
  exec R "${@}"

%apprun Rscript
  exec Rscript "${@}"

%runscript
  exec R "${@}"

%environment
%post
  DEBIAN_FRONTEND=noninteractive
